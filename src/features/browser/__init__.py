"""
Feature `browser` — Điều khiển Chrome qua extension: tab, media, mở-hoặc-dùng-lại.

Package để trống ở cấp module (xem `features/places/__init__.py` giải thích lý do);
khai báo Feature nằm ở `feature.py`.
"""
