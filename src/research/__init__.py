"""
Lõi RESEARCH — trung lập về miền (L3-1 của `docs/research_lane_spec.md`).

Ở đây KHÔNG được biết gì về địa điểm: không toạ độ, không bán kính, không Maps. Phần
riêng của địa điểm nằm ở `actions/place_identity.py` và các module `actions/place_*`.

Ranh giới này là ràng buộc đã chốt với người dùng (spec §5): địa điểm làm trước, nhưng
lõi phải cắm được miền khác (sản phẩm, khoá học...) mà không phải viết lại.
"""
