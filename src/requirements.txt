# Voice Assistant - Python dependencies (target: Python 3.9+)
#
# Version floors (>=) are conservative. After installing and running successfully on your machine,
# you should lock the exact version using: pip freeze > requirements.lock.txt
#
# NOTE: tkinter, webbrowser, ctypes are standard Python libraries - DO NOT install via pip.

# --- Audio: recording & speech synthesis ---
PyAudio>=0.2.11
numpy>=1.24.0
gTTS>=2.3.0
pyttsx3>=2.90
pygame>=2.5.0

# --- Speech Recognition (STT) ---
SpeechRecognition>=3.10.0
openai-whisper>=20231117
faster-whisper>=0.10.0
torch>=2.0.0              # dependency of openai-whisper

# --- Agent LLM (tool-calling) ---
# Default uses local LLM via Ollama (install Ollama app separately, not via pip;
# called using requests which is already listed above). Only 'anthropic' is needed if LLM_PROVIDER=claude.
anthropic>=0.40.0        # optional - only when using Claude API

# --- Web Control / Search ---
requests>=2.31.0         # call Ollama + fetch page (web_fetch) + videoId YouTube + thời tiết Open-Meteo

# --- Điều khiển Chrome (tùy chọn: BROWSER_BRIDGE_ENABLED=true) ---
websockets>=13.0         # cầu nối WebSocket tới Chrome extension (API asyncio mới)

# --- Giao diện ---
# Bắt buộc: avatar hiện nhân vật bằng ảnh PNG và phải thu ảnh theo kích thước cửa sổ
# (Tk PhotoImage chỉ thu được theo bội số nguyên). Panel HUD cũng dùng để hiện ảnh
# địa điểm. `packaging/make_avatar_frames.py` cần thêm numpy (đã có ở trên).
Pillow>=10.0.0

# --- Configuration (optional: allows loading .env files) ---
python-dotenv>=1.0.0

# --- System Control (Windows) ---
psutil>=5.9.0
pycaw>=20230407           # volume control (Windows)
comtypes>=1.2.0           # dependency of pycaw
screen-brightness-control>=0.16.0
