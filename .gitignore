# Python bytecode
__pycache__/
*.py[cod]
*$py.class

# Models (downloaded/cached, do not commit)
/src/models/

# Virtual environments
.venv/
venv/
env/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local data / logs
*.log
src/services/reminders.json
src/services/tasks.json
src/services/routines.json
src/services/contacts.json

# Hồ sơ người dùng (dữ liệu cá nhân: tên, địa điểm...) — local, không commit
src/components/user/user_data/
# Dữ liệu bộ nhớ của trợ lý (hồ sơ người dùng, nhân cách) — RIÊNG TƯ, không đưa lên repo
src/memory/data/
# Claim cache của Lane 3 — nội dung web đã đọc, sinh lại được, không thuộc mã nguồn
src/research/data/

# Log runtime khi chạy qua launcher (launch_assistant.vbs) — stdout+stderr, ghi đè mỗi lần
assistant.log

# Log của logger, xoay vòng, sống qua nhiều lần chạy (LOG_FILE)
logs/

# Ảnh chụp màn hình (tính năng đọc màn hình) — local, không commit
captures/

# Bộ dữ liệu huấn luyện giọng riêng: giọng THẬT của người dùng (dữ liệu sinh trắc, không
# thu hồi được sau khi đã lên mạng) + model tải về. ~385 MB, TUYỆT ĐỐI không commit.
my_dataset_builder/

# Bí mật / khoá / gói build — KHÔNG commit
.env
.env.*
!.env.example
*.pem
# OAuth Google (MCP server cá nhân) — cấp quyền vào tài khoản, TUYỆT ĐỐI không commit
mcp_servers/credentials.json
mcp_servers/token*.json
*.crx

# Extension Chrome + tài liệu — giữ cục bộ, không commit
chrome_extension/
docs/
